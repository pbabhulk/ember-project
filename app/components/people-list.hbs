<h2>{{@title}}</h2>

{{#each @people as |person|}}
    <li>
        <button {{on 'click' (fn this.showPerson person)}}>{{person}}</button>
        </li>
    <h2>Im called from PeopleList component</h2>
{{/each}}